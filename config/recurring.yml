# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

production:
  clear_solid_queue_finished_jobs:
    command: "SolidQueue::Job.clear_finished_in_batches(sleep_between_batches: 0.3)"
    schedule: every hour at minute 12

  cleanup_expired_entries:
    class: CleanupExpiredEntriesJob
    schedule: at 3am every day

  cleanup_expired_jobs:
    class: CleanupExpiredJobsJob
    schedule: at 3:30am every day

  send_weekly_stats:
    class: SendWeeklyStatsJob
    schedule: every sunday at 9am

  retry_stuck_jobs:
    class: RetryStuckJobsJob
    schedule: every 15 minutes

  send_daily_digest:
    class: SendDailyDigestJob
    schedule: at 8am every day
